# 关节、骨段与人物外观

## 低头身手指截面修复

用户指出低比例手指侧面扁薄。检查源顶点、低头身目标和手部骨架变换，确认 createSoftTarget 的肌肉平滑只取 X/Z 平滑坐标、保留原 Y，细手指截面因此严重失衡；男性、女性中指取样的 Z 截面分别只剩约 24.5% 和 16.7%。此处复用 local spatial.scene-authoring 的同拓扑目标及既有蒙皮，修复已承诺的低头身手脚体量，合约维持 9。比较模型、Agent 骨架与 PNG 消费路径后，不新增独立变形规则、存储字段、UI 文案或模型类型。

按原有手部蒙皮权重保留手掌／手指 X/Z 截面，在手腕混合到前臂的平滑效果。二、三头身继续使用后续的三轴保体积平滑，四头身使用相同修复后的参考，七头身源网格保持不变。修复后男女中指取样的 Z 截面分别保留约 96.2% 和 94.1%，Y 保留约 93.3% 和 84.5%。只修正目标顶点，手部长度、宽度控制曲线、关节位置、骨段旋转和固定不改动，复制手臂自动复用。

新增男女双手的中指截面回归测试，先复现失败，再约束二／三／四头身目标的两个截面维度均保留源值的 80%–120%。整只手的包围盒不能暴露单根手指变扁，因此取样以源手部解剖位置和指轴横截面为准。隔离手部诊断图 hand-volume-{before,after}-{male,female}-{front,side,quarter}.png 从左至右为 2、2.5、3、3.5、4、7；腕部切口来自独立模块诊断，不代表整个人物断开。整个人物衔接与摆姿见 hand-volume-body-*。未修改用户空间数据。

验证通过：空间 7 文件 44 项、共享合约 131 通过／1 跳过；领域与 i18n 检查、完整类型检查、服务端／Next／Storybook 构建及包检查通过。真实 WebGL 三头身右手近景确认手指体积及腕部衔接，控制台无错误或警告；截图为 hand-volume-workspace.png。

## 二、三头身专用目标网格

用户批准改进低头身的整体风格。本轮继续归属 local 的 spatial.scene-authoring；比较现有 morph/model、世界关节解算、Agent inspect 和 PNG 消费路径，复用既有骨架、操作、存储及摄像机输出，不新增持久化字段或模型类型。领域合约升为 9。语义继续复用 spatial.proportions 与 bodyType 的规范记录和中英文键，含义及控件保持一致，无新增文案。

二、三头身分别通过独立的脸部／躯干轮廓控制点生成同拓扑目标顶点。二头身填充脸颊、缩短下半脸并简化眉眼鼻唇，胸腹接近一个连续圆润体积；三头身使用较收窄的脸颊与轻微腰线。四肢使用平顺的截面，增加细弱小腿、手掌和脚部的体量，腿部轮廓跨膝连续。轮廓变形后焊接源网格重合顶点并执行保体积平滑，避免仅移动截面而保留成人纵向细节造成折痕。目标顶点在每个男女源模型载入时生成一次，再按统一参考权重插值；这属于基于原网格的轮廓重塑，不是外部人工精雕资产。

2.5、3.5 等小数同时插值目标顶点与骨架尺寸，头高仍直接取 height / headRatio，继续保留用户躯干腿部分配、姿势及锁定。四头身接原来的半软化参考，七头身接原始网格；成人源资产不改写。编辑器的部位网格保持独立选择，但相接身体皮肤累加共享法线，消除圆润躯干上的模块光照接缝，自定义分支按各自前缀分开。PNG 与编辑器仍使用同一套顶点；服务端 PNG 延续原有逐三角形明暗，WebGL 使用平滑法线，二者光照表现并非逐像素一致。

验收检查：男女二／三头身的头宽与头高关系、腰胸轮廓差异、小腿／脚部体量；摆姿后跨部位和 UV 接缝法线一致；既有小数连续性、站姿重心、头高、落地、关节接缝、独立固定、自定义肢体、往返成人形态与 Agent 操作测试。视觉证据沿用 candidate / pending-user-review，不能将方向批准视为最终视觉验收。

验证通过：7 个空间测试文件共 42 项；共享合约 131 通过、1 跳过；领域基线与 i18n 检查、完整类型检查、服务端／Next／Storybook 构建及包检查均通过。实际浏览器在 1536×960 检查男女三头身正面、圆润皮肤接缝，小数比例与躯干比例／固定交互完成且无控制台错误。截图 chibi-workspace.png 来自真实 WebGL；chibi-{male,female}-{front,side,quarter}.png 每排从左至右为 2、2.5、3、3.5、4、7；chibi-*-poses.png 为 2.5 与 3.5 的抬手、蹲下、踢腿；另有 chibi-custom-2.5.png 与 chibi-custom-3.5.png 的四臂双头。均为隔离 fixture，未修改用户空间。

## 低头身默认站姿对齐

用户指出侧面默认站姿的头、身体与脚部不在自然竖线上。实际世界坐标确认二头身腰部向前偏移约 8.5–8.8cm，原因是前后截面厚度被同时用于放大脊柱偏移，缩短颈部的深度补偿又叠加到腰胸。比较 spatial.scene-authoring 的关节解算、蒙皮、Agent inspect 与 PNG 后，修复继续归属该 local 能力；版本及镜头引用沿用既有共享存储与消费路径。标准解剖数据仍存储原有偏移，不做迁移或改写姿势。

低头身把骨架中线与皮肤厚度分开：关节前后偏移回归纵向缩放，颈部缩短只补足纵向高度，核心蒙皮按关节轴线剪切重定向，避免用旋转补偿厚度而抬起/扭斜整个横截面；随现有连续形态权重过渡到七头身原形态。复用 spatial.proportions 与 reset-pose 的原语义，不增加或更改中英文 UI 文案。验证将覆盖男女 2/2.5/3/3.5/4 默认站姿、躯干比例边界、连续性、固定约束和已有摆姿，并目视比较修复前后的正侧面。

领域合约升级为 8。新增回归测试先确认男女模型的腰部偏移/高度比超过 0.55，再验证修复后在全部低头身及躯干比例边界内小于 0.25，并约束真实腰部网格相对骨盆的前后中心偏移。1.8m 默认二头身的腰部偏移由男性 8.75cm、女性 8.46cm 降至约 2.62cm、2.57cm；自然曲线保留，不修改已有骨段动作角度。核心剪切仅用于符合底模解剖轴线的部位，自定义斜向头部/肢体连接继续沿自身骨架方向变形。

验证通过：7 个空间测试文件共 38 项；共享合约 131 通过/1 跳过；领域基线、i18n、完整类型检查、Next/Storybook 构建及包检查。实际浏览器小数头身、躯干比例和固定冲突交互均完成，无控制台错误或警告；隔离 Story 另直接检查二头身的实际 WebGL 侧面。新正侧面、摆姿和自定义模型证据保存在 docs/evidence/spatial/stance-*.png，原 fractional-* 证据保留用于前后比较。未修改用户空间数据，未新增 UI 控件或文案。

## 连续低头身形态（当前）

用户补充侧面也需协调头与身体的体量。形态参考增加独立的头部前后厚度和躯干深度，低头身收住后脑与面部的前后尺寸，并为胸背、腹部保留厚度。沿用同一头身参数自动联动，不增加控件或文案；头高基准、正面宽度和用户躯干腿部分配保持既定职责。头身变化时的深度偏移同样进入世界关节与网格计算，确保侧面、斜侧与俯仰角一致。

用户已批准 2、3、4 头身参考形态与成人形态之间连续过渡，允许 2.5、3.5 和一位小数输入。复用 spatial.scene-authoring 的 set-proportions、现有模型蒙皮和世界关节计算；比较 DraftVersionStore、composition.camera-reference 后，形态仍属于空间领域，存储历史与构图镜头继续委托既有实现。无新持久化字段或格式转换。头部基准长度直接取 height / headRatio，参考形态只插值归一化的脸型、颈长、肩胯、躯干及四肢尺寸；保留用户躯干／腿部分配与骨段姿势，固定冲突仍原子拒绝。

复用 spatial.proportions 的语义记录与中英文键，先补充其连续形态含义，再改实现；InputNumber 使用 0.5 步进和一位小数，维持与身高并排。沿用已批准的本地 SpatialWorkspaceView、AppShellFrame 与 Ant Design 控件，不新增页面模式或通用组件。当前形态规则替代下方历史记录中的统一低头身缩放曲线；7 头身保持源模型，以上保留修长曲线。验证覆盖男女正侧面 2/2.5/3/3.5/4/7、非半步值、抬手/蹲下/踢腿、自定义部位、固定与共享输出；视觉证据仍标为 candidate，不能把实现方向批准等同最终视觉验收。

本轮验证：空间 7 文件 36 项通过，共享合约 131 通过/1 跳过；领域、语义、类型、Next 与 Storybook 构建及包检查通过。FractionalProportionsInteractions 实际浏览器验证 2.5→方向键 3.0、3.5 和 2.7 保存，身高/躯干腿部分配保持；ProportionsInteractions 的 2% 预览、提交与固定冲突回归通过。两个 Story 均 phase finished，控制台无错误或警告。三个受支持桌面尺寸无横向溢出，身高与头身比例保持并排。正侧面、斜侧姿势及四臂双头 PNG 已目视检查，文件位于 docs/evidence/spatial/fractional-*.png。头高基准是准确的骨架长度，皮肤下巴同时受颈部蒙皮权重影响；仍保留源模型五官风格和自定义部位拼接边界。

实际 WebGL 界面已检查 2.5 头身男性与 3.5 头身女性并排显示，证据为 docs/evidence/spatial/fractional-workspace.png。YisiUI doctor 与 diff 空白检查通过。

按用户已批准方向扩展本地 SpatialWorkspaceView / SpatialViewport。继续复用 AppShellFrame 右栏、TabSwitch、AssetSkeletonTree、BasicButton、InputNumber、Checkbox、Select；构图和 UI 的二维画布不具备三维骨架责任，不重复其布局框架。两个标签与人物下部位平级结构保留。新增关节位置/骨段旋转选择、六个角度边界、复原、人物外观和编辑骨架显示。

领域比较了 spatial.scene-authoring、capture.draft-versioning 和 composition.camera-reference。版本与撤销继续委托 DraftVersionStore；空间骨架、约束、模型变形仍为 local；构图仅消费既有摄像机 PNG，不复制变形逻辑。

语义记录先于文案：腰部关节指位置点，腰部指绕上游点旋转的骨段；局部旋转与世界位置区分；复原明确清除固定且可撤销。中文和英文分别表达，复用原有世界位置、锁定、对象与摄像机标签。

在现有 fixture Story 中验证默认、四臂、双头、固定与复原、错误、加载、空、禁用、长内容、英文；桌面参考 1024×800、1280×800、1536×960。沿用 candidate / pending-user-review 状态，不宣称稳定或视觉验收。

## 验证结果

- `spatial.scene-authoring` 合约升级到 2；新增关节/骨段、旧版读取、复原与模型网格的领域测试。版本写入、并发与撤销继续使用共享 DraftVersionStore；未增加平行持久化规则。
- 语义和中英文资源同步；`i18n:check` 通过（488 个键，487 个语义记录，1 个原有遗留键）。
- 3D 专项：5 个测试文件，21 项通过。覆盖旋转轴心、独立分支、固定点/方向、六个角度边界、复原保留形态、旧版姿势及容量上限、真实网格复制、确定性 PNG、保存/撤销。
- 领域基线检查通过；共享合约测试 131 项通过、1 项跳过。TypeScript、Next 生产构建、Storybook 构建、YisiUI doctor 和包文件检查通过。
- 最近一次全量测试中保留 2 项既有失败：构图 CLI 方法数期待 5 而实际为 9；createI18n 的旧文案期待“加工”而当前为“精修”。本轮没有修改这两个无关断言。
- 实际浏览器在 1024×800、1280×800、1536×960 下无横向溢出，300px 右栏在屏幕内并可内部滚动，摄像机 PNG 正常加载。Story 的平级树、统一行高、自动部位工具、独立固定、键盘操作、复原和 Escape 交互全部完成，控制台无错误。
- 隔离空间的实际 UI：手部旋转保存为 revision 3，固定为 4，复原为 5（角度归零、解除固定），撤销为 6，与 revision 4 的文档完全一致。未编辑用户现有空间。
- 视觉证据：`docs/evidence/spatial/bone-rotation-model.png`（实际 WebGL 选择与旋转环），`quaternius-camera.png`、`quaternius-four-arms.png`、`quaternius-two-heads.png`（共享渲染器的实际 PNG）。四臂和双头已人工检查轮廓与连接。
- 已知呈现边界：纯色蒙皮外观；复制肢体的连接处为拼接，不保证无缝/封闭拓扑。未实现手指姿势或姿势库。

## 最新调整：单一模型与独立对象标签

用户已明确旧空间删除完毕，不需要转换。删除旧人物几何分支、外观切换、v1 schema、转换模块和旧格式 fixture；领域仍由 spatial.scene-authoring 管理，合约升至 3，只接受当前 v2 / Quaternius。共享版本存储与构图镜头消费边界保持原有委托关系。

复用 AppShellFrame、TabSwitch、AssetSkeletonTree，使用现有本地 View 承载三个标签：信息与工具、空间对象、参数。对象标签只含树，移除 320px 列表上限；树选择留在对象页，画布选择与新建打开参数。状态覆盖延续原有 Story，补充标签隔离、空参数提示、选择和展开保留。同步资产/页面 Registry。用户直接指定的现有布局调整无需引入新的页面模式或重复 UI 组件。

语义复用空间对象与参数的规范键（标签和同名分区共用），移除合并标签及外观选择、旧头部连接骨段概念；新增无选择时的参数指引。中文和英文同步维护。

本轮验证：i18n 检查通过（484 键、483 语义记录、1 既有遗留键）；领域基线检查及共享合约测试通过（131 通过、1 跳过）；空间 5 个测试文件共 20 项通过，包括拒绝旧格式、真实模型网格、镜头输出、固定与复原撤销。TypeScript、Next 生产构建、Storybook 构建、YisiUI doctor、包文件检查及 diff 空白检查通过。

实际浏览器 Story 交互完成（phase finished），覆盖三个标签、对象和参数互斥、选择/展开保留、固定、键盘编辑、复原、新建摄像机自动打开参数。英文界面直接点击画布中人物，自动打开 Properties 中的关节参数。1024×800、1280×800、1536×960 均无页面横向溢出；英文三个标签在 260px 可用宽度内完整显示。长内容对象树高度 1519px，右栏滚动区 704px，可完整浏览全部对象；对象页不含参数或镜头预览。静态 Storybook 出现一项资源 404，交互和 WebGL 加载正常。

## 躯干／腿部比例控件

按用户批准的交互扩展现有 SpatialWorkspaceView。继续复用 AppShellFrame、BasicButton、InputNumber 和现有 Ant Design provider；共享及本地 Registry 按 slider 查询无匹配职责，使用 Ant Design Slider 组合在既有本地参数区，不创建重复通用组件。两端标记躯干和腿部及对应百分比，滑块位置对应真实占比，只有中间 30%–70% 可拖动。头身比例与身高并排，既有语义键复用；新增比例、端点、提示、复原比例及固定冲突语义，中英文同步。预览不写入版本，松手或键盘操作结束提交一次，选中变化清除预览。

领域继续复用 spatial.scene-authoring 的 set-proportions；与共享 DraftVersionStore、构图镜头引用边界比较后，比例计算保留在空间域，不新增路由或持久化实现。标准与自定义人物共用模型部位含义，保留姿势及固定、头与手脚尺寸；通过模型的默认形态计算分配，不根据当前姿势计算。桌面 1024×800、1280×800、1536×960，覆盖标准/四臂/双头、两端限制、固定冲突、比例复原、英文、禁用和独立预览。

本轮验证通过：空间专项 6 文件 26 项；共享合约 131 通过、1 跳过；领域基线、i18n（490 键，489 语义，1 既有遗留）、TypeScript、Next 与 Storybook 构建、YisiUI doctor、包检查。实际 ProportionsInteractions Story 结束且控制台无错误，覆盖 Home/End 范围、真实分界位置、预览不提交、结束一次提交、恢复默认比例和固定冲突。英文鼠标拖动验证文档不变，松手后才改变比例；中英文两字段同一行。支持的三个桌面尺寸均无横向溢出。颈部保持长度，两端实际 PNG 已目视检查且躯干、腿部均可见，证据在 docs/evidence/spatial/body-proportions-{long,short}.png。

## 2% 步进与头身形态联动

复用现有 Ant Design Slider，把可选档位设为 30%、32%…70%。模型默认值约 40.4% 或 Agent 写入的非整步值仍准确显示，通过隐藏的当前值标记避免控件自行舍入；开始调整后进入 2% 档位。延续并排字段、真实分界位置、预览与结束提交行为，不增加新控件或页面模式。

头身比例继续复用 spatial.proportions 规范键，中英文标签含义一致；语义记录补充身体宽厚联动，无须更名。spatial.scene-authoring 合约升为 5；比较已有模型、关节解算、CLI inspect 与摄像机输出后，统一使用形态后的关节偏移。存储偏移描述基础解剖数据，解算时按头身数调整肩胯间距及躯干深度，固定验证和网格沿用同一结果，无新增版本或迁移实现。以 7 头身为基准使用 sqrt(7 / headRatio) 渐进调整身体宽厚，避免低头身等倍膨胀；躯干与横向连接按解剖坐标缩放，四肢绕骨轴调整截面。

空间领域测试累计 28 项通过，覆盖 2/4/7/10/12 头身的肩胯宽度、躯干与腿部宽厚、总身高、有限网格坐标，自定义手臂/头部、姿势与固定保留、位置固定冲突原子拒绝、比例往返恢复和编辑/PNG 一致性。共享合约 131 项通过、1 项跳过；领域基线、语义检查、类型检查、Next/Storybook 构建通过。正面/侧面实际输出见 docs/evidence/spatial/head-proportions-{front,side}.png，左至右为 2、4、7、10、12 头身；另目视检查四臂、双头与踢腿组合。模型在极低头身下仍会有明显挤压褶皱，当前不涉及更换网格或重绘蒙皮权重。

最终浏览器交互 Story 完成且控制台无错误：默认 40.4% 的数值、ARIA 与比例文字一致，左右方向键按 2% 预览/提交，Home/End 保持 30%–70%，复原与固定冲突行为通过。1024×800、1280×800、1536×960 无页面横向溢出，中英文身高与头身比例仍并排。YisiUI doctor、包检查、diff 空白检查通过。

## 女性底模与低头身收细

按用户指定继续使用已下载的免费 Quaternius Superhero 系列，加入女性 glTF/bin，并通过同一个转换脚本生成女性网格和解剖数据。复用既有 SpatialWorkspaceView、AppShellFrame 和参数区 Select；与现有摄像机下拉框比较后沿用相同控件与间距，不新建页面或通用组件。人物外形选择独立于比例、姿势和颜色；语义先记录 body-type/body-male/body-female，再增加中英文规范键“人物外形／Body model、男性／Male、女性／Female”，不将其作为用户身份属性。

spatial.scene-authoring 合约升到 6，新增可选 bodyType=male/female；未记录字段的当前文档按男性读取，保留当前 v2，无旧格式迁移。add-character 与 set-proportions 复用创建及比例计算，并按源模型选择解剖尺寸；切换保留姿势、锁定与自定义部位，冲突继续原子拒绝，版本及撤销继续委托共享 DraftVersionStore。相比单独修改网格字段，统一操作同时保持关节位置、Agent inspect、编辑网格和 PNG 一致。

低头身宽度规则按最新要求调整：2–7 头身使用 sqrt(headRatio / 7)，7–12 头身沿用 sqrt(7 / headRatio)，参考点 7 连续。肩胯间距、躯干截面和手臂、手掌、腿脚一起收细，不再低头身反向膨胀；本节替代上一节关于低头身加宽的规则。男女源模型都使用同一曲线，各自保留原模型形态差异。

验证通过：空间专项累计 31 项；共享合约 131 通过、1 跳过；领域/语义门禁（493 键、492 语义、1 既有遗留键）、TypeScript、Next 与 Storybook 构建、YisiUI doctor、包文件及 diff 空白检查。新增覆盖男女 2/4/7/10/12 头身的肩胯间距及各肢段真实网格截面、身高/躯干腿部边界、女性源三角面和头部网格、外形切换往返、固定冲突、四臂/双头、API 创建与撤销、CLI 世界坐标和 PNG 输出。原有头身宽度断言按用户要求改为低头身变窄，保持 7 以上修长曲线验证。

实际浏览器 BodyTypeInteractions 与 ProportionsInteractions 完成且控制台无错误，验证男性切女性保留四臂踢腿、固定手腕后切回被阻止、默认精确值与 2% 步进、一次提交、比例复原。正面证据 male-slim-proportions.png / female-slim-proportions.png 从左至右为 2/4/7/10/12 头身；female-custom-pose.png 展示女性四臂双头与踢腿；侧面实际 PNG 同样完成目视检查。

桌面 1024×800、1280×800、1536×960 均无页面横向溢出，300px 右栏内头身比例和身高保持同一行；英文 Body model 可选择 Female，控制台无错误或警告。

## 摄像机范围与预览比例

复用现有 SpatialViewport、SpatialWorkspaceView 和 AppShellFrame 右栏；Registry 按 camera 能力查询无独立匹配，既有本地空间视口承担该职责，二维 CompositionCanvas 不适合三维取景范围。只扩展视图呈现，沿用 createOutputCamera 的投影参数，不修改空间操作、保存或 PNG 输出规则。选中摄像机时显示浅色 CameraHelper，范围延伸至观察目标所在平面，随位置、目标、视野/取景高度和输出宽高更新；清除选择或选中其他对象后移除。辅助线不参与拾取或输出。

预览图保持摄像机指定宽高比，同时受右栏宽度和 230px 高度约束，纵向画幅居中。沿用 spatial.camera-view / spatial.select-camera 的已有语义与中英文规范键，仍指已保存摄像机的输出和选择操作；无新增文案或翻译键。验证状态包括横向、纵向、正方形、透视/正交、多镜头切换、取消选择与桌面受限面板。

验证通过：Web/Storybook 类型检查、Next 生产构建、Storybook 构建、YisiUI doctor、i18n 检查（493 键、492 语义、1 既有遗留）与 diff 空白检查。CameraPreviewInteractions 实际浏览器交互完成，覆盖镜头切换、宽高修改和 Escape，控制台无错误或警告。实测横向预览约 244.67×137.63px，纵向 129.375×230px，已加载旧横向图片时外框仍保持当前设定比例。1024×800、1280×800、1536×960 均无页面横向溢出。

通过浏览器临时 Three.js DevTools 观察器检查实际编辑场景：范围宽高比随 16:9 → 9:16 改变；透视 60° 对应目标深度处正确高度，正交取景高度 4 对应 2.25×4 的 9:16 范围；位置与观察目标同步，始终只存在一个选中辅助框，更新释放旧几何，Escape 后辅助框归零。辅助框未加入可拾取内容或共享输出场景。横向/纵向截图已目视检查，纵向桌面证据见 docs/evidence/spatial/camera-frame-portrait.png。

## 五指关节接入

沿用已确认的 AppShellFrame 右栏三标签与 AssetSkeletonTree：人物下仅手部增加容器，手下按五指顺序平铺关节和骨段，身体其余部位仍同级；不用骨架父子关系直接生成深层树。继续使用实验发布中已采用的 TabSwitch、BasicButton、Ant Design Slider/InputNumber；不另造树或旋转工具。手部选择、五指弯曲/张开/拇指内收与单手复原属于现有参数面板扩展，不改变整体视觉方向。Story 覆盖标准/低头身、男女、四臂、英文、禁用、树层级和键盘控制，检查桌面内部滚动。

领域归属仍为 local `spatial.scene-authoring`：比较并复用 applySpatialOperations、solveJoint、validateSpatialTransition、共享 DraftVersionStore 和 createCharacterModel。手指解算限定本指；手部快捷参数原子地修改现有骨段角度，世界坐标/旋转固定仍硬校验。标准五指恢复 Quaternius 原有蒙皮，编辑与 PNG 共用几何。只补全当前 v2 Quaternius 的手部，不接受退役火柴人或 v1 格式。命名在 spatial 语义分片记录手部容器、掌心位置点、手掌骨段、指节、弯曲和内收的不同作用域，覆盖中英文。

五指交付证据：

- `docs/evidence/spatial/finger-tree.png`：手下关节与骨段平铺，按指根—骨段—下一关节的顺序排列，指尖可选。
- `docs/evidence/spatial/finger-parameters.png`：整手选择、局部旋转、五指弯曲、张开、内收与单手复原。
- `docs/evidence/spatial/finger-poses-male-3-{palm,quarter}.png`、`finger-poses-female-2-quarter.png`、`finger-poses-male-7-quarter.png`：相同原始权重与形态算法下的五种动作对照，左起默认伸展、张开、五指弯曲、保留食指伸展、保留拇指伸展。这组是独立手部模块诊断，手腕切口来自模块截取；完整人物的手腕连接见工作区截图。

领域契约更新为 local `spatial.scene-authoring@10`；使用共享 `DraftVersionStore`，没有新增存储/撤销实现。手指补全只针对当前 v2 Quaternius，新增测试确认原身体姿势与原手尖世界位置固定保留；未引入火柴人兜底。共享契约 131 通过、1 跳过。空间相关 56 测试通过，覆盖骨骼/蒙皮、2/3/7 头身指尖解算、左右手张开、拇指基部运动、位置与旋转固定冲突、单手复原、四臂复制、非法参数、CLI 方法与历史 PNG。中英文语义检查、完整类型检查、YisiUI doctor、Next/Storybook 生产构建与包清单检查通过。

浏览器验证 HandInteractions（含默认、最小桌面尺寸）、BodyTypeInteractions、LockInteractions，检查中英文参数、三个支持桌面尺寸的内部滚动与无横向溢出。后台标签页会限制树的展开动画，交互测试需在前台运行。截图与交互证据不代替用户最终视觉验收，Surface 保持 candidate/pending-user-review。


## 人物颈长、手掌、手指和脚部尺寸

复用 SpatialWorkspaceView 的人物参数、AppShellFrame 右栏和已采用的 Ant Design Slider/InputNumber，保留头身/身高并排和原有三标签。比较现有躯干比例滑杆、手部动作控件及公共 YisiUI 资产；这四项是人物形态，采用既有比例预览与提交边界，不放入单手动作参数或二维构图控件。没有新增视觉方向或共享组件；实验资产状态与 pending-user-review 保持不变。窄桌面面板通过内部滚动容纳字段，英文标签与图标同排。

语义分片先记录脖子长度、手掌大小、手指长度、脚部相对大小四个独立概念及默认百分比/左右同步提示，随后写入中英文。手掌与手指长度不复用整手动作、掌心坐标或关节旋转命名。领域使用 local spatial.scene-authoring@11 的同一比例操作、固定校验与蒙皮；DraftVersionStore 沿用原有保存/撤销。

BodyShapeInteractions 覆盖四项唯一人物级控件、范围与 100% 默认值、预览不写入、松手只提交一次、2% 键盘步进和数值恢复默认；桌面 1024×800、1280×800、1536×960 范围检查内部滚动及无横向溢出。原有 ProportionsInteractions 和 HandInteractions 已在前台浏览器回归；额外检查英文与禁用状态。尺寸边界的正面、侧面和独立手掌/指长组合渲染，见 docs/3D空间实现.md 的证据清单。

最终验证：新增尺寸及指尖 IK 测试使空间相关测试共 65 项通过；共享契约 131 通过、1 跳过。语义检查 519 键/518 语义/1 既有遗留，完整类型检查、YisiUI doctor、Next/Storybook 构建和包清单检查通过。最终面板证据为 `docs/evidence/spatial/body-dimensions-panel.png`，最终 BodyShapeInteractions 完成且控制台无错误或警告。


## 颈长与脚踝行为修正

按用户反馈更新 local spatial.scene-authoring@12：显式颈长只移动头部，不通过躯干补偿维持总身高；脚部变长变宽时适度增厚，脚踝跟随升降，小腿连接随之更新，身体/膝盖位置与骨段角度保持。仍复用原有参数控件、比例操作、约束校验、共享版本存储和编辑/PNG 几何。沿用脖子长度与脚部大小的中英文标签，在语义分片修正其行为含义；没有改变页面布局或命名。独立颈长/脚部对照见 docs/evidence/spatial/neck-length-head-position.png、foot-size-ankle-height.png。

新增/修正测试覆盖躯干骨架和网格不变、头部随已摆好的颈部方向移动、双头及额外腿部、温和脚踝升降、脚底误差范围，以及头部/脚踝固定冲突与胸部固定可继续调整颈长。空间相关 67 项测试通过，共享契约 131 通过、1 跳过。

修正后的完整类型检查、领域/语义检查、Next/Storybook 生产构建及包清单检查通过；BodyShapeInteractions 前台浏览器回归完成，控制台无错误或警告。
